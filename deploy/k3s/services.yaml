apiVersion: v1
kind: Service
metadata:
  name: core
  namespace: coc
spec:
  selector:
    app: core
  ports:
    - name: http
      port: 8000
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: chat
  namespace: coc
spec:
  selector:
    app: chat
  ports:
    - name: http
      port: 8001
      targetPort: 8001
---
apiVersion: v1
kind: Service
metadata:
  name: ai
  namespace: coc
spec:
  selector:
    app: ai
  ports:
    - name: http
      port: 8002
      targetPort: 8002
---
apiVersion: v1
kind: Service
metadata:
  name: chroma
  namespace: coc
spec:
  selector:
    app: chroma
  ports:
    - name: http
      port: 8000
      targetPort: 8000
